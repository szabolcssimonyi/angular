import { urls, routes, paths } from './environment.common';

export const environment = {
  backendProvider: true,
  production: true,
  apiHost: '.',
  apiPort: ':8760',
  apiUrl: './api/',
  urls,
  routes,
  paths
};
